services:
  - type: web
    name: classifiche-campionati
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
      # Aggiungi VITE_BETSAPI_TOKEN da Render Dashboard → Environment → Add Variable
